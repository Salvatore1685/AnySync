package com.routersync.app

/**
 * Password per sbloccare la navigazione libera sull'intero HDD (oltre la cartella di
 * sincronizzazione). Va cambiata qui nel codice sorgente prima di compilare l'app:
 * non è visibile né modificabile dall'interno dell'app stessa, solo da chi ha accesso
 * al codice e lo builda (tu).
 *
 * IMPORTANTE: cambia questo valore con una password a tua scelta prima di generare l'APK.
 */
const val ADMIN_UNLOCK_PASSWORD = "CambiaQuestaPassword123"
